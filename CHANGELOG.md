## 0.1.0

* Removed `flutter_riverpod` dependency.
* Updated `NixButton` state management to use local `ValueNotifier` and `ValueListenableBuilder` performance optimizations.
* Apps containing `NixButton` are no longer required to be wrapped in a `ProviderScope`.

## 0.0.1

* Initial release (relying on Riverpod).
